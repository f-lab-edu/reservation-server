bootJar {
    enabled = true
    archiveBaseName = 'reservation-supplier-api'
}

jar {
    enabled = false
}

dependencies {
    implementation project(':reservation-common')

    implementation 'org.springframework.boot:spring-boot-starter-web'
}