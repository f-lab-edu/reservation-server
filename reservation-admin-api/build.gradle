bootJar {
    enabled = true
    archiveBaseName = 'reservation-admin-api'
}

jar {
    enabled = false
}

dependencies {
    implementation project(':reservation-common')
    implementation project(':reservation-auth')

    implementation 'org.springframework.boot:spring-boot-starter-web'
}